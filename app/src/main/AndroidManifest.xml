<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizhandballowy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ikona"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ikona"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".teacheng"
            ></activity>
        <activity android:name=".quizowanieeng"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".English_main" />
        <activity android:name=".result"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".teach"
            />
        <activity
            android:name=".quizowanie"
            android:label="@string/title_activity_quizowanie"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>